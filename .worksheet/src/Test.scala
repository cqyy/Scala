import Element._

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(59); val res$0 = 
 	elem(Array("Hello","Hi"));System.out.println("""res0: Element = """ + $show(res$0))}
           
}
